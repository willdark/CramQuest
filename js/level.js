class Level {
    constructor(levelDef, stage) {
        this.__levelDef = levelDef;
        this.__stage = stage; //will need the stage so that we can add our objects
    }

    intialize() {
        //add content to stage
    }

    start() {
        //start the level
    }

    complete() {}


}